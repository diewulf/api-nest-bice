// crea un dto llamado FallabellaPdfDto con los campos: clientName, amount, dateExpired, fallabellaSodimac, giftcardTottus, giftcardNumber, securityCod 

export class FallabellaPdfDto {
  clientName: string;
  amount: number;
  dateExpired: Date;
  fallabellaSodimac: string;
  giftcardTottus: string;
  giftcardNumber: string;
  securityCod: string;
}